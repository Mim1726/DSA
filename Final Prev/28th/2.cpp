Consider a disjoint set data structure considering the elements 1-10(inclusive).

(a) Perform the following operations without path compression. You must
    draw the trees in each step.
    a.union(1,9)   b.union(2,4)   c.union(4,5)   d.union(6,3)
    e.union(6,7)   f.union(6,8)   g.union(9,6)
    
(b) Perform the following operations with path compression. You must
    draw the trees in each step.
    a.union(1,9)   b.union(2,4)   c.union(4,5)   d.union(6,3)
    e.union(6,7)   f.union(6,8)   g.union(9,6)
